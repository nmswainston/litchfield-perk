backend:
  name: git-gateway
  branch: main

media_folder: "public/uploads"
public_folder: "/uploads"

publish_mode: simple

collections:
  - name: "menu"
    label: "Menu"
    files:
      - name: "menuData"
        label: "Menu"
        file: "src/data/menu.json"
        format: "json"
        fields:
          - label: "Categories"
            name: "categories"
            widget: "list"
            summary: "{{name}} ({{id}})"
            fields:
              - { label: "ID", name: "id", widget: "string", disabled: true, hint: "System identifier — cannot be changed" }
              - { label: "Name", name: "name", widget: "string" }
              - { label: "Short Name", name: "shortName", widget: "string" }

          - label: "Items"
            name: "items"
            widget: "list"
            summary: "{{name}} ({{category}})"
            fields:
              - { label: "ID", name: "id", widget: "string" }
              - { label: "Name", name: "name", widget: "string" }
              - { label: "Description", name: "description", widget: "text", required: false }
              - { label: "Price", name: "price", widget: "number", required: false }
              - label: "Category"
                name: "category"
                widget: "select"
                options:
                  - { label: "Espresso/Coffee", value: "espresso" }
                  - { label: "Tea/Non-Espresso", value: "tea" }
                  - { label: "Desserts", value: "desserts" }
              - { label: "Popular", name: "popular", widget: "boolean", default: false }

